# Drop your own Apple corecrypto.zip here as `corecrypto.zip` to build without
# the build reaching out to developer.apple.com ("bring your own corecrypto").
# The zip is .gitignored and must never be committed (Apple's corecrypto
# Internal Use License forbids redistributing the source).
